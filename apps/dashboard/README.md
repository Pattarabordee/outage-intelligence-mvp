# Dashboard placeholder

For this public-safe MVP, the main FastAPI app exposes a lightweight HTML dashboard at `/demo/incidents`.

A production implementation could split this into a separate frontend (Next.js, React, or Streamlit) and add:
- stakeholder SLA views
- real-time incident timelines
- confidence indicators for ETA revisions
- backlog / site risk heatmaps
